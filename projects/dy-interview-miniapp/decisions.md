# dy-interview-miniapp Decisions

## 2026-04-28

- Use a native Douyin mini program implementation instead of embedding the Web page.
- Keep the first version focused on the public landing page from `interview.puritynexus.com`.
- Store the logo locally as `images/logo.png` in the code repository.
- Use `tt.showToast` placeholders for login/register/footer links until target flows are defined.
- Keep progress and cross-device coordination notes in `codex--midtrans`, separate from business source code.
